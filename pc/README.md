# Grape Windows build

This directory contains the Windows-specific Grape source. It is part of the
GPLv3 corresponding source and is built with the repository root sources.

```sh
export GEODE_SDK=/path/to/GeodeSDK
bash build.sh
```
